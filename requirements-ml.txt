# Production embedder: local Hugging Face sentence-transformers (pulls torch).
# Heavy, so it's kept out of the base image; install only where you run the
# real embedder (ingestion with --write, or the API on the pgvector backend
# with embedder=sentence-transformer).
-r requirements.txt
sentence-transformers==3.3.1

# Vision generation: local Qwen2.5-VL (generator=qwen-vision). transformers>=4.49
# is required for the Qwen2.5-VL classes. Also heavy; same install boundary.
transformers>=4.49.0
qwen-vl-utils==0.0.8
accelerate>=1.0.0
pillow>=10.0.0
